#include <rojo/graphics/detail/default_image_loader.hpp>

namespace rojo
{
    namespace detail
    {
        bool default_image_loader::load(image_data& data, const std::string& path)
        {
            // todo
            return false;
        }

        bool default_image_loader::save(const image_data& data, const std::string& path)
        {
            // todo
            return false;
        }
    }
}
